# 和欧調整ツール 仕様

Notofit JP の設計値を目視で決めるための開発支援ツール（GUI）の仕様。
配布物ではなく、M2（設計値の確定）のための道具である。

計画書は [plan.md](plan.md)。

---

## 1. 目的

`SCALE` / `BASELINE_OFFSET` / ウェイトの組み合わせは、単体では正解が存在せず、和文と
欧文を並べて初めて「高い / 低い / 細い」が判断できる。しかも3つが相互に影響する
（欧文を拡大すれば太く見えるため `wght` を戻したくなる）。

この往復を目視で行うための調整器とする。

## 2. 位置づけ

**成果物は設定値（JSON）であり、フォントそのものではない。**

```
和欧調整ツール ──> 設定 JSON ──> ビルドスクリプト ──> dist/
   （値を決める）              （配布物を作る）
```

ツールが出力した値を合成ツールの config に流し込む形とし、責務を重ねない。ツールを
経由しなくてもビルドは回る状態を保つ。

## 3. 担当範囲

**原則: 2つのフォントを並べて見ないと決められない値だけを扱う。**

### 範囲内

| パラメータ           | 内容                                       |
| -------------------- | ------------------------------------------ |
| `SCALE`              | 欧文の拡縮                                 |
| `BASELINE_OFFSET`    | 欧文の上下位置                             |
| 和文 / 欧文の `wght` | 見かけの太さを揃える組み合わせ             |

いずれも **出力ウェイトごとに個別の値を持つ**。当面は 400 / 700 の2ウェイトだが、
将来の追加が差分で済むよう、設定は最初からウェイトごとの配列とする。

### 範囲外

| 項目                       | 理由                                                                 |
| -------------------------- | -------------------------------------------------------------------- |
| グリフ個別の垂直調整（④）  | 対象が数十グリフあり、別種の UI が必要。混植の全体調整とは作業が異なる |
| `palt` の焼き込み比率      | 和文内部の話であり、和欧の関係ではない（→ 4）                          |
| `excludeCodepoints`        | 記号ごとの字形の好みを一覧で選ぶ作業。混植プレビューと無関係           |
| サブセット化・CSS 生成・配布 | ビルドの責務                                                          |

## 4. `palt` の扱い

**調整対象ではなく、プレビューの表示条件とする。**

理由は2つ。

1. 約物のアキは Noto 単体で完結して判断でき、欧文を並べる必要がない。3. の原則に照らすと
   範囲外になる
2. この値の決め手は R5 のトレードオフ（焼き込むほど組版は良くなるが、既存サイトの
   レイアウト差分が大きくなる）であり、**「どれだけ字送りが変わるか」という数値**が
   支配的。目視は最後の確認にすぎない。連続スライダーにすると、この制約を見失いやすい

ただし焼き込み比率によって和文の見え方は変わるため、プレビューには反映されている
必要がある。**候補値のプリセット切り替え**（0 / 0.34 / 0.5 / 1.0 など数段階）とし、
どの前提で見ているかを常に明示する。

## 5. プレビューの固定条件

調整対象ではないが、本番と同じ条件で見るために固定するもの。

| 条件                      | 値                                                      |
| ------------------------- | ------------------------------------------------------- |
| `text-autospace`          | `normal`（常時オン）                                    |
| 書字方向                  | 横書きのみ                                              |
| `palt` 焼き込み比率       | プリセットから選択（→ 4）                                |

`text-autospace` をオフの状態で `BASELINE_OFFSET` を詰めると、本番と違うものを見て
判断することになる。

## 6. 未確定事項

- 実装形態（ローカルで動かす Web ページか、ブラウザ内で完結させるか）
- プレビューに使うサンプル文章
- 調整結果を JSON として書き出す方法（クリップボード / ファイル）
